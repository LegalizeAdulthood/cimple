
Errors I made while writing this provider:

    1. Added duplicate property (detected at runtime).

    2. Misspelled property (never detected by CIMOM).

    3. Forgot to inject object path into new instances.
