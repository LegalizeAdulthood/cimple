This directory creates the options.h file located in the cimple/src/cimple
directory.  It is executed as part of compilation any time the
config.options file is changed to insure that any changes to the
configuration are incorporated into the build.
