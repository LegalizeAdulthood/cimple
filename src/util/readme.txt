This library is for utilities shared across all of the command line tools
(genclass, genprov, file2c, etc.). Do not link this into the CIMPLE engine
or providers since it links in the standard C++ types (string, vector).
