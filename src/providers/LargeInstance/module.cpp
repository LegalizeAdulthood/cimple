//==============================================================================
//
// PLEASE DO NOT EDIT; THIS FILE WAS AUTOMATICALLY GENERATED BY GENMOD 2.1.0
//
//==============================================================================

/* NOCHKSRC */
#include <cimple/cimple.h>
#include "CMPL_LargeInstance_Provider.h"

using namespace cimple;

static int __cimple_CMPL_LargeInstance_Provider_proc(
    const Registration* registration,
    int operation,
    void* arg0,
    void* arg1,
    void* arg2,
    void* arg3,
    void* arg4,
    void* arg5,
    void* arg6,
    void* arg7)
{
    typedef CMPL_LargeInstance Class;
    typedef CMPL_LargeInstance_Provider Provider;

    if (operation != OPERATION_INVOKE_METHOD)
        return Instance_Provider_Proc_T<Provider>::proc(registration,
            operation, arg0, arg1, arg2, arg3, arg4, arg5, arg6, arg7);

    Provider* provider = (Provider*)arg0;
    const Class* self = (const Class*)arg1;
    const char* meth_name = ((Instance*)arg2)->meta_class->name;

    if (strcasecmp(meth_name, "setConfig") == 0)
    {
        typedef CMPL_LargeInstance_setConfig_method Method;
        Method* method = (Method*)arg2;
        return provider->setConfig(
            method->type,
            method->size,
            method->count,
            method->return_value);
    }

    if (strcasecmp(meth_name, "getConfig") == 0)
    {
        typedef CMPL_LargeInstance_getConfig_method Method;
        Method* method = (Method*)arg2;
        return provider->getConfig(
            method->type,
            method->size,
            method->count,
            method->return_value);
    }

    if (strcasecmp(meth_name, "test") == 0)
    {
        typedef CMPL_LargeInstance_test_method Method;
        Method* method = (Method*)arg2;
        return provider->test(
            method->return_value);
    }

    return -1;
}

CIMPLE_MODULE(CMPL_LargeInstance_Module);
CIMPLE_INSTANCE_PROVIDER(CMPL_LargeInstance_Provider);

#ifdef CIMPLE_PEGASUS_MODULE
  CIMPLE_PEGASUS_PROVIDER_ENTRY_POINT;
# define __CIMPLE_FOUND_ENTRY_POINT
#endif

#ifdef CIMPLE_CMPI_MODULE
  CIMPLE_CMPI_INSTANCE_PROVIDER(CMPL_LargeInstance_Provider);
  CIMPLE_CMPI_INSTANCE_PROVIDER2(CMPL_LargeInstance_Provider, CMPL_LargeInstance);
# define __CIMPLE_FOUND_ENTRY_POINT
#endif

#ifdef CIMPLE_OPENWBEM_MODULE
  CIMPLE_OPENWBEM_PROVIDER(CMPL_LargeInstance_Module);
# define __CIMPLE_FOUND_ENTRY_POINT
#endif

#ifdef CIMPLE_WMI_MODULE
# include "guid.h"
  CIMPLE_WMI_PROVIDER_ENTRY_POINTS(CLSID_CMPL_LargeInstance_Module)
# define __CIMPLE_FOUND_ENTRY_POINT
#endif

#ifndef __CIMPLE_FOUND_ENTRY_POINT
# error "No provider entry point found. Please define one of the following: CIMPLE_PEGASUS_MODULE, CIMPLE_CMPI_MODULE, CIMPLE_OPENWBEM_MODULE, CIMPLE_WMI_MODULE"
#endif
