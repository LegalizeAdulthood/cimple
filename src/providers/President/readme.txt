This provider implements a simple class (President) and only the get_instance
and enumerate operations.

I implements both the enumerate_instances and get_instances functions.

There is no defined test and currently the provider is not compiled as
part of the automated test suite.

