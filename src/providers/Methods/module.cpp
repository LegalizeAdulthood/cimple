//==============================================================================
//
// PLEASE DO NOT EDIT; THIS FILE WAS AUTOMATICALLY GENERATED BY GENMOD 2.1.0
//
//==============================================================================

/* NOCHKSRC */
#include <cimple/cimple.h>
#include "Methods_Provider.h"

using namespace cimple;

static int __cimple_Methods_Provider_proc(
    const Registration* registration,
    int operation,
    void* arg0,
    void* arg1,
    void* arg2,
    void* arg3,
    void* arg4,
    void* arg5,
    void* arg6,
    void* arg7)
{
    typedef Methods Class;
    typedef Methods_Provider Provider;

    if (operation != OPERATION_INVOKE_METHOD)
        return Instance_Provider_Proc_T<Provider>::proc(registration,
            operation, arg0, arg1, arg2, arg3, arg4, arg5, arg6, arg7);

    Provider* provider = (Provider*)arg0;
    const Class* self = (const Class*)arg1;
    const char* meth_name = ((Instance*)arg2)->meta_class->name;

    if (strcasecmp(meth_name, "foo") == 0)
    {
        typedef Methods_foo_method Method;
        Method* method = (Method*)arg2;
        return provider->foo(
            self,
            method->arg1,
            method->arg2,
            method->arg3,
            method->arg4,
            method->arg5,
            method->arg6,
            method->return_value);
    }

    if (strcasecmp(meth_name, "foo2") == 0)
    {
        typedef Methods_foo2_method Method;
        Method* method = (Method*)arg2;
        return provider->foo2(
            self,
            method->arg1,
            method->arg2,
            method->return_value);
    }

    if (strcasecmp(meth_name, "foo3") == 0)
    {
        typedef Methods_foo3_method Method;
        Method* method = (Method*)arg2;
        return provider->foo3(
            method->return_value);
    }

    if (strcasecmp(meth_name, "foo4") == 0)
    {
        typedef Methods_foo4_method Method;
        Method* method = (Method*)arg2;
        return provider->foo4(
            self,
            method->arr1,
            method->arr2,
            method->return_value);
    }

    if (strcasecmp(meth_name, "foo5") == 0)
    {
        typedef Methods_foo5_method Method;
        Method* method = (Method*)arg2;
        return provider->foo5(
            self,
            method->in_arg,
            method->in_out_arg,
            method->out_arg,
            method->return_value);
    }

    if (strcasecmp(meth_name, "junk") == 0)
    {
        typedef Methods_junk_method Method;
        Method* method = (Method*)arg2;
        return provider->junk(
            self,
            method->x,
            method->y,
            method->return_value);
    }

    return -1;
}

CIMPLE_MODULE(Methods_Module);
CIMPLE_INSTANCE_PROVIDER(Methods_Provider);

#ifdef CIMPLE_PEGASUS_MODULE
  CIMPLE_PEGASUS_PROVIDER_ENTRY_POINT;
# define __CIMPLE_FOUND_ENTRY_POINT
#endif

#ifdef CIMPLE_CMPI_MODULE
  CIMPLE_CMPI_INSTANCE_PROVIDER(Methods_Provider);
  CIMPLE_CMPI_INSTANCE_PROVIDER2(Methods_Provider, Methods);
# define __CIMPLE_FOUND_ENTRY_POINT
#endif

#ifdef CIMPLE_OPENWBEM_MODULE
  CIMPLE_OPENWBEM_PROVIDER(Methods_Module);
# define __CIMPLE_FOUND_ENTRY_POINT
#endif

#ifdef CIMPLE_WMI_MODULE
# include "guid.h"
  CIMPLE_WMI_PROVIDER_ENTRY_POINTS(CLSID_Methods_Module)
# define __CIMPLE_FOUND_ENTRY_POINT
#endif

#ifndef __CIMPLE_FOUND_ENTRY_POINT
# error "No provider entry point found. Please define one of the following: CIMPLE_PEGASUS_MODULE, CIMPLE_CMPI_MODULE, CIMPLE_OPENWBEM_MODULE, CIMPLE_WMI_MODULE"
#endif
