Early provider test with some array properties.  Uses Instance_map as
the provider pattern so that instances can be added, removed, and 
modified.

Note that tests are very limited at this point and it is not part of
the regression tests.

