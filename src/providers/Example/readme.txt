This provider defines a single class with a number of different methods
and implements the methods.

There are only methods in the Example class so that the provider does
not support enumerate, get, create, delete instances.

