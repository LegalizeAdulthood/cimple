TOP = ..
include $(TOP)/mak/config.mak

all:
	@ $(ECHO) "CIMPLE_PLATFORM=$(CIMPLE_PLATFORM)"
	@ $(ECHO) "CIMPLE_ROOT=$(CIMPLE_ROOT)"
	@ $(ECHO) "BIN_DIR=$(BIN_DIR)"
	@ $(ECHO) "LIB_DIR=$(LIB_DIR)"
	@ $(ECHO) "SHLIB=$(SHLIB)"
	@ $(ECHO) "SRC_DIR=$(SRC_DIR)"
	@ $(ECHO) "INCLUDES=$(INCLUDES)"
	@ $(ECHO) "CXX=$(CXX)"
	@ $(ECHO) "CC=$(CC)"
	@ $(ECHO) "AR=$(AR)"
	@ $(ECHO) "AR_OPTS=$(AR_OPTS)"
	@ $(ECHO) "FLAGS=$(FLAGS)"
	@ $(ECHO) "CC=$(CC)"
	@ $(ECHO) "DEFINES=$(DEFINES)"
	@ $(ECHO) "VALGRIND=$(VALGRIND)"
	@ $(ECHO) "LINK_FLAGS=$(LINK_FLAGS)"
	@ $(ECHO) "RPATH=$(RPATH)"
	@ $(ECHO) "RPATH_OPT=$(RPATH_OPT)"
	@ $(ECHO) "STATIC_LIBSTDCXX=$(STATIC_LIBSTDCXX)"
	@ $(ECHO) "LD_LIBRARY_PATH=$(LD_LIBRARY_PATH)"
	@ $(ECHO) "SIZE_OPTIMIZATION_FLAGS=$(SIZE_OPTIMIZATION_FLAGS)"
	@ $(ECHO) "MKDIRHIER=$(MKDIRHIER)"
	@ $(ECHO) "PWD=$(PWD)"
	@ $(ECHO) "SIZE=$(SIZE)"
	@ $(ECHO) "OBJ=$(OBJ)"
	@ $(ECHO) "GCC_VERSION=$(GCC_VERSION)"
